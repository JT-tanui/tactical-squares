name: chess_coach_ai
description: A premium chess coaching platform with AI integration.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  flutter_chess_board: ^1.0.1
  chess: ^0.7.0
  google_fonts: ^4.0.4
  http: ^0.13.6
  firebase_core: ^2.13.1
  firebase_auth: ^4.6.2
  firebase_analytics: ^10.4.2
  cloud_firestore: ^4.8.0
  flutter_blue_plus: ^1.5.2
  web_socket_channel: ^2.4.0
  wifi_iot: ^0.3.18
  shared_preferences: ^2.1.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/

